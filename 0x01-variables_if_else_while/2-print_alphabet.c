#include <stdio.h>
/**
  *main- entry point
  *
  *Return:always 0
  */
int main(void)
{
	char letter = 'z';

	while (letter <= 'z')

	putchar(letter);
	letter++;
	{
		putchar(letter);
		return (0);
	}
}
