#include <stdio.h>
/**
  *main - entry point of program
  *
  *Return:always 0
  */
int main(void)
{
	char = c;

	while (c <= 'z')
	{
	putchar(c);
	c++;
	}
	else if (c != q && != e)
		putchar(c);
		c++;
		{
			putchar('\n');
			return (0);
		}
}
